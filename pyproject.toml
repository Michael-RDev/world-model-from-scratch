[project]
name = "world-model-from-scratch"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Michael-Rusu", email = "89427972+Michael-Rusu@users.noreply.github.com" }
]
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.0",
    "opencv-python>=4.10",
    "pygame>=2.6",
]

[project.scripts]
world-model-from-scratch = "world_model_from_scratch:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
